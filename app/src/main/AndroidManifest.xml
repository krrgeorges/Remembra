<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.remembra">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ConvertFilesActivity"
            android:theme="@style/Dialogos"></activity>
        <activity android:name=".CTViewerActivity" />
        <activity
            android:name=".ConvertTextActivity"
            android:theme="@style/Dialogos" />
        <activity
            android:name=".ConvertOptionsActivity"
            android:theme="@style/Dialogos" />
        <activity
            android:name=".SearchActivity"
            android:theme="@style/Dialogos" />
        <activity
            android:name=".GoToActivity"
            android:theme="@style/Dialogos" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".SettingsActivity" />
        <activity
            android:name=".ViewerActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity
            android:name=".FileChooser"
            android:theme="@style/Dialogos" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>